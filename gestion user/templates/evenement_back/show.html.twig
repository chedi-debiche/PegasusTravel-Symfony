{% extends 'baseBacK.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idevent</th>
                <td>{{ evenement.idevent }}</td>
            </tr>
            <tr>
                <th>Nomevent</th>
                <td>{{ evenement.nomevent }}</td>
            </tr>
            <tr>
                <th>Prixevent</th>
                <td>{{ evenement.prixevent }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evenement_back_index') }}">back to list</a>

    <a href="{{ path('app_evenement_back_edit', {'idevent': evenement.idevent}) }}">edit</a>

    {{ include('evenement_back/_delete_form.html.twig') }}
{% endblock %}
