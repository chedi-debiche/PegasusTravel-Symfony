<form method="post" action="{{ path('app_reservationv_delete', {'idr': reservationv.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationv.idr) }}">
    <button class="btn">Delete</button>
</form>
