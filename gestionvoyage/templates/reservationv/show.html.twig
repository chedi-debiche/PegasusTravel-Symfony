{% extends 'baseFront.html.twig' %}

{% block title %}Reservationv{% endblock %}

{% block body %}
    <h1>Reservationv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idr</th>
                <td>{{ reservationv.idr }}</td>
            </tr>
            <tr>
                <th>NbPersonnes</th>
                <td>{{ reservationv.nbPersonnes }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservationv.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservationv_index') }}">back to list</a>

    <a href="{{ path('app_reservationv_edit', {'idr': reservationv.idr}) }}">edit</a>

    {{ include('reservationv/_delete_form.html.twig') }}
{% endblock %}
